# BenefitPulse — frontend

React + Vite UI for the BenefitPulse Engine.

```bash
npm install
npm run dev
```

Opens at http://localhost:5173. The Vite proxy forwards `/api` to `http://localhost:8000` unless you set `VITE_API_URL` in `.env`.

See the [root README](../README.md) for full setup.
